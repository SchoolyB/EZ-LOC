module utils

const LineType enum {
    CODE = 0,
    COMMENT,
    BLANK
}

const CodeFileType enum {
    C = 0
    // C_CPP_HEADER
    CPP
    PYTHON
}

//TODO: implement this shit for easier file info tracking
const CodeFile struct {
    fullpath string
    name string // files name
    extension string // files extension
    type CodeFileType
    size byte
}


const CodeFileMap map[CodeFileType:string] = {
	CodeFileType.C: "c",
	CodeFileType.PYTHON: "py"
}